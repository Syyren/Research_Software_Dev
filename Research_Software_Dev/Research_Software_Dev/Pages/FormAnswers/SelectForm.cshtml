@page "{participantId}/{sessionId}"
@model Research_Software_Dev.Pages.Forms.SelectFormModel
@{
    ViewData["Title"] = "Select Form";
}

<h1>Select Form</h1>

<form method="post">
    <!-- Hidden fields to carry participant and session data -->
    <input type="hidden" asp-for="ParticipantId" />
    <input type="hidden" asp-for="SessionId" />

    <!-- Form Selection -->
    <div class="form-group mb-2">
        <label asp-for="FormId">Form</label>
        <select asp-for="FormId" class="form-control" required>
            <option value="">-- Select a Form --</option>
            @foreach (var form in Model.Forms)
            {
                <option value="@form.FormId">@form.FormName</option>
            }
        </select>
        <span asp-validation-for="FormId" class="text-danger"></span>
    </div>

    <!-- Submit Button -->
    <button type="submit" class="btn btn-outline-primary">Next</button>
</form>
